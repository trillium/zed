[package]
name = "command_server"
version = "0.1.0"
edition = "2021"

# Standalone package (not part of Zed workspace yet)
[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = "1.0"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3.0"
